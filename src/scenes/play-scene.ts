export default class PlayScene extends Phaser.Scene {
  public static readonly SCENE_KEY = 'PlayScene';

  constructor () {
    super({ key: PlayScene.SCENE_KEY })
  }

  init () { }

  preload () { }

  create () { }

  update () { }
}